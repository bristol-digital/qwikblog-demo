# ────────────────────────────────────────────────────────────────────────────
#  QwikBlog
# ────────────────────────────────────────────────────────────────────────────

# Admin login credentials. Both are required — login is refused if either
# is empty. There is no default password.
ADMIN_USERNAME=
ADMIN_PASSWORD=

# Posts directory. Leave empty to use resources/posts under the host app.
# QWIKBLOG_POSTS_PATH=

# How long parsed posts stay in the cache (seconds). Writes through the
# admin or bulk-import command always invalidate the cache regardless.
# QWIKBLOG_CACHE_DURATION=3600

# RSS <language> tag. Leave empty to use the application locale.
# QWIKBLOG_LANGUAGE=

# Reading speed (words per minute) for the "X min read" indicator.
# QWIKBLOG_READING_WPM=200

# Posts per page on the public blog (index, search, filters, archives).
# QWIKBLOG_PER_PAGE=12

# Posts per page in the admin posts table.
# QWIKBLOG_ADMIN_PER_PAGE=30

# Maximum items in the RSS feed at /blog/feed.xml.
# QWIKBLOG_FEED_LIMIT=50

# Admin URL prefix (default: /admin). Override to obscure the URL.
# QWIKBLOG_ADMIN_PATH=admin

# Taxonomy URL style:
#   flat     — /blog/{slug} resolves as post → category → tag → 404
#   prefixed — category and tag URLs at /blog/category/{slug}, /blog/tag/{slug}
# QWIKBLOG_TAXONOMY_URL_STYLE=flat

# Related posts scoring:
#   score = (shared_tags × tag_weight) + (shared_categories × category_weight)
# QWIKBLOG_RELATED_TAG_WEIGHT=2
# QWIKBLOG_RELATED_CATEGORY_WEIGHT=1
# QWIKBLOG_RELATED_LIMIT=3
